{
  "name": "profile",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:images": "imagemin images/**/* --out-dir=dist/assets/",
    "serve": "vite preview",
    "start": "npm run dev"
  },
  "devDependencies": {
    "imagemin-cli": "^6.0.0",
    "vite": "^2.3.3"
  },
  "dependencies": {
    "three": "^0.128.0"
  }
}
